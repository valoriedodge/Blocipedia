<div>
<% unless @wiki.collaborators.empty?  %>
  <% @wiki.collaborators.each do |collaborator| %>
  <%= collaborator.email %>
  <%#= link_to "Remove Collaborator", wiki_collaborator_path(@wiki, collaborator), method: :delete, class: 'small', data: {confirm: 'Are you sure you want to delete this collaborator?'} %> <br/>
  <%  end %>
<% end %>
</div>
